With ``ensure_exists``, the ``site_*_dir`` and ``site_*_path`` properties and the ``iter_*_dirs`` iterators only create
the site directories they return or yield, instead of every entry in the site list - by :user:`darrenhuai`.
