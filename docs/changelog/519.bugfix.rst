Resolve ``user_desktop_dir`` on Windows when ``ctypes`` is unavailable - ``CSIDL_DESKTOPDIRECTORY`` was missing from
both the registry and environment variable resolvers, so the property raised ``ValueError`` instead of returning a path.
